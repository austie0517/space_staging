# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: stress.spec.ts >> 同時アクセス負荷テスト
- Location: e2e/tests/stress.spec.ts:30:1

# Error details

```
Test timeout of 30000ms exceeded.
```

```
Error: page.goto: net::ERR_ABORTED; maybe frame was detached?
Call log:
  - navigating to "https://staging.enbria.space/host/spaces/00000000-0000-0000-0000-000000000106", waiting until "domcontentloaded"

```

# Test source

```ts
  1  | /**
  2  |  * Vercel(サーバーレス)が同時アクセスに耐えられるかを見るための負荷テスト。
  3  |  * Login/Stripeは未実装なので、認証不要なページのみを巡回する。
  4  |  *
  5  |  * 実行: npx playwright test --config=playwright.config.stress.ts
  6  |  *
  7  |  * 各リクエストのステータス・所要時間を [stress] プレフィックスでログ出力するので、
  8  |  * 実行直後に Vercel の Logs タブ（または `vercel logs --follow`）を見比べると、
  9  |  * P1001 / P2024 / 5xx がこのタイミングで出ていないか確認できる。
  10 |  */
  11 | 
  12 | import { test, expect } from '@playwright/test';
  13 | 
  14 | const GUEST_FLOW = [
  15 |   '/spaces',
  16 |   '/spaces/00000000-0000-0000-0000-000000000106',
  17 |   '/spaces/00000000-0000-0000-0000-000000000107',
  18 | ];
  19 | 
  20 | const HOST_FLOW = [
  21 |   '/host/spaces',
  22 |   '/host/spaces/00000000-0000-0000-0000-000000000106',
  23 |   '/host/bookings',
  24 |   '/host/earnings',
  25 | ];
  26 | 
  27 | // 1往復で終わらせず、しばらく負荷をかけ続けたい場合はここを増やす
  28 | const REPEAT = Number(process.env.STRESS_REPEAT ?? 1);
  29 | 
  30 | test('同時アクセス負荷テスト', async ({ page }, testInfo) => {
  31 |   const isHost = testInfo.project.name.startsWith('host');
  32 |   const flow = isHost ? HOST_FLOW : GUEST_FLOW;
  33 | 
  34 |   for (let i = 0; i < REPEAT; i++) {
  35 |     for (const path of flow) {
  36 |       const start = Date.now();
> 37 |       const response = await page.goto(path, { waitUntil: 'domcontentloaded' });
     |                                   ^ Error: page.goto: net::ERR_ABORTED; maybe frame was detached?
  38 |       const elapsed = Date.now() - start;
  39 |       const status = response?.status() ?? 0;
  40 | 
  41 |       console.log(`[stress] ${testInfo.project.name} ${path} -> ${status} (${elapsed}ms)`);
  42 | 
  43 |       expect(status, `${testInfo.project.name}: ${path} が5xxを返した`).toBeLessThan(500);
  44 |     }
  45 |   }
  46 | });
  47 | 
```