// ============================================================================
//  Phase 2 DRAFT (退避) — このままでは schema.prisma に入れてもコンパイル不可。
//  問題点:
//   - id が cuid() / String (Phase 1・既存DBは uuid @db.Uuid)
//   - @@map / @map が無い → 既存 snake_case テーブルに一致しない（新テーブルを作る）
//   - 逆リレーション欠落 (Booking.coupon, User.hostProfile/supportTickets/payouts/favorites,
//     Space.images(重複)/favorites)
//   - SpaceImage は Phase 1 で既に定義済み → 重複
//   - HostProfile / SupportTicket / Payout は DB に存在しないテーブル
//   - 既存 coupons / reviews / favorites は別の列構成で実在
//  → 正式 Phase 2 を作るときは uuid + snake_case + 逆リレーションを付けて作り直す。
// ============================================================================

model Coupon {
  id          String    @id @default(cuid())
  code        String    @unique
  discount    Int
  expiresAt   DateTime
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt

  bookings    Booking[]
}

model Review {
  id        String   @id @default(cuid())
  bookingId String   @unique
  rating    Int
  comment   String?
  createdAt DateTime @default(now())

  booking   Booking  @relation(fields: [bookingId], references: [id])
}

model HostProfile {
  id        String   @id @default(cuid())
  userId    String   @unique
  bio       String?
  phone     String?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  user      User     @relation(fields: [userId], references: [id])
}

model SupportTicket {
  id        String   @id @default(cuid())
  userId    String
  subject   String
  message   String
  status    String
  createdAt DateTime @default(now())

  user      User     @relation(fields: [userId], references: [id])
}

model Payout {
  id        String   @id @default(cuid())
  hostId    String
  amount    Int
  status    String
  createdAt DateTime @default(now())

  host      User     @relation(fields: [hostId], references: [id])
}

model SpaceImage {
  id        String   @id @default(cuid())
  spaceId   String
  url       String
  createdAt DateTime @default(now())

  space     Space    @relation(fields: [spaceId], references: [id])
}

model Favorite {
  id        String   @id @default(cuid())
  userId    String
  spaceId   String
  createdAt DateTime @default(now())

  user      User     @relation(fields: [userId], references: [id])
  space     Space    @relation(fields: [spaceId], references: [id])
}
